package Recursion;

public class Fibo_Formula {
    public static void main(String[] args) {
        int n=3;
        int febo=(int)(Math.pow(((1+ Math.sqrt(5))/2), n) / Math.sqrt(n));
        System.out.println(febo);
    }
}
